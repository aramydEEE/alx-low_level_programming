#include "main.h"
/**
 * _memset - checks for upper case letter
 * @s: function's parameter
 * @b: second parameter
 * @n: third parameter
 * Return: A character
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
